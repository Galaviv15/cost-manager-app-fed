import { createTheme } from "@mui/material/styles";

// Custom theme aligned with a finance-oriented palette.
var theme = createTheme({
  palette: {
    primary: {
      main: "#0b2d4d"
    },
    secondary: {
      main: "#d4a017"
    },
    background: {
      default: "#f4f6f8"
    }
  }
});

export default theme;
